<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.quannaapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ReviewActivity"></activity>
        <activity android:name=".SampleComplete" />
        <activity android:name=".SampleActivity" />
        <activity android:name=".ConnectToDeviceActivity" />
        <activity android:name=".LocationActivity" />
        <activity android:name=".BestFitActivity" />
        <activity android:name=".MyHistoryActivity" />
        <activity android:name=".MyProfileActivity" />
        <activity android:name=".thirdActivity" />
        <activity android:name=".SeconedActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>